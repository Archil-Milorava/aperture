# Copy this file to .env and fill in real values.
NODE_ENV=development
PORT=3000

# Database (PostgreSQL) — these must match docker-compose.yml
DB_HOST=localhost
DB_PORT=5432
DB_USER=aperture
DB_PASSWORD=change_me
DB_NAME=aperture
